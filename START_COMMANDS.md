# Start Commands

From repository root:

```powershell
cd "D:\TRAE项目\stocks_lxy-main"
npm.cmd start
```

Or:

```powershell
cd "D:\TRAE项目\stocks_lxy-main"
node scripts/dev.js
```

Project source directory:

```powershell
cd "D:\TRAE项目\stocks_lxy-main\stock-board-journal"
npm.cmd start
```

Default URLs:

- Frontend: `http://localhost:3002`
- Backend health: `http://localhost:3001/api/health`
